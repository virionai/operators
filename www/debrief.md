# WWW Debrief

The app opens directly into the operational workspace. `Overview.html` is the current lightweight overview artifact, not a separate website.

A public competition page can be added later if needed, but it should not block or obscure the offline product surface, one-line startup, Ollama/Gemma local runtime expectations, or known Capsule export limitations.
